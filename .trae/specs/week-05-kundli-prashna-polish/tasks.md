# Week 05: Core polish for Kundli and Prashna - Implementation Plan

## Task 1: Kundli — Diversify loading skeletons (summary bar + planet table)
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit `src/pages/BirthChartPage.tsx`, the loading block currently at lines 338-341.
  - Replace the single `LoadingSkeleton variant="card" rows={6}` with TWO stacked skeletons that visually preview what arrives:
    1. **ChartSummaryBar skeleton**: A 3-column grid (`grid grid-cols-3 gap-3`) with 3 individual `LoadingSkeleton variant="card"` blocks OR (to keep it simple and match design tokens) a single Skeleton row: three rounded bordered tiles using inline `<div>` with `h-[80px] animate-pulse rounded-xl border border-border bg-card` classes.
    2. **PlanetTable skeleton**: A `LoadingSkeleton variant="table" rows={10}` (9 planets + Ascendant is typical) placed after the summary skeleton.
  - Wrap both in a `space-y-4` container to match the result section's `space-y-5` visual rhythm.
  - Keep existing `mb-4` outer margin behavior.
- **Acceptance Criteria Addressed**: AC-1
- **Test Requirements**:
  - `rule` TR-1.1: BirthChartPage.tsx loading section renders at minimum two distinct skeleton blocks — one for the summary row (or equivalent 3 tiles) AND one table-style skeleton; evidence: source code showing at least one instance each of either (a) `variant="table"` + `grid-cols-3` wrapper with skeleton tiles, or (b) equivalent structural skeleton per AC-1
  - `rule` TR-1.2: Skeleton block is still placed inside the same aria-live result div (lines 331-395) and conditionally rendered only when `{isLoading && ...}`; evidence: source inspection
  - `rubric` TR-1.3: Structural faithfulness dimension; scale 1-5; anchors 1 = single unchanged card, 3 = 2 skeletons present but summary is generic lines not 3-column tile preview, 5 = both structures clearly preview the actual ChartSummaryBar 3-tile layout and PlanetTable rows/columns look; threshold >=4; evidence: visual diff at 1024px

## Task 2: Kundli — Add Print + Share/Copy reading summary actions
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit `src/pages/BirthChartPage.tsx` result header area (currently lines 375-383, the flex row with h2 + Ayanamsa span).
  - Add a button group (flex wrap) aligned right in the header, OR placed below the PlanetTable in a new action row with `flex flex-wrap justify-end gap-2`.
  - Implement two bilingual buttons (EN+HI with `isHi` ternary labels):
    1. **Print / Save** button using existing Share/Print icons — use `MessageSquareQuote` icon (already imported in QuestionPage to see pattern) or `Printer` if available. The handler calls `window.print()`.
    2. **Copy / Share** button using Share2 icon from lucide-react — calls `navigator.clipboard.writeText(...)` with a structured multiline summary: `Birth Chart for ${name}\nLagna: ${lagna}\nMoon: ${moon}\nSun: ${sun}\nAyanamsa: ${val}\nGenerated by Vedic Rajkumar.`. Adds a `copied` state (useState<boolean>) that changes label text to "Copied" / "कॉपी हुआ" for ~1800ms.
  - Import `Share2` and `MessageSquareQuote` (or `Printer`) icons from lucide-react as needed.
  - Use existing Button/variant="outline" size="sm" styling pattern from QuestionPage.share buttons (L1117-1126).
- **Acceptance Criteria Addressed**: AC-2
- **Test Requirements**:
  - `rule` TR-2.1: Result area contains at least 2 `<Button>` actions wired to `window.print()` and `navigator.clipboard.writeText(...)` respectively; evidence: source code grep
  - `rule` TR-2.2: Both button labels have EN (isHi=false) and HI (isHi=true) variants with `isHi ? "...hi..." : "...en..."` ternary; evidence: source inspection
  - `rule` TR-2.3: Share/Copy button has transient "Copied" state via useState; evidence: copied state variable definition + setTimeout clear present
  - `rubric` TR-2.4: Placement aesthetics dimension; scale 1-5; anchors 1 = buttons crowd the heading, 3 = buttons present but vertically awkward, 5 = buttons group positioned cleanly at result footer end or right-align in header without clashing with ayanamsa text; threshold >=4

## Task 3: Kundli — Add bilingual labels for the 7 form fields and 6 PlanetTable column headers
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit `src/pages/BirthChartPage.tsx`.
  - **Form labels (7 total)**: For each existing `<Label htmlFor="...">EN</Label>`, turn its child content into an `isHi ? "Hindi" : "English"` ternary and add `font-hindi` class when Hindi. Standard Hindi translations to use: Name="नाम", Date of Birth="जन्म तिथि", Time of Birth (24h)="जन्म समय (24 घंटे)", Latitude (decimal)="अक्षांश (दशमलव)", Longitude (decimal)="रेखांश (दशमलव)", IANA Timezone="IANA समय क्षेत्र", Place of Birth="जन्म स्थान".
  - **PlanetTable column headers (6 total)**: At `<thead>` lines 64-72, convert each `<th>` text content into an `isHi` ternary and apply `font-hindi` for Hindi. Standard Hindi: Planet="ग्रह", Sign="राशि", House="भाव", Nakshatra="नक्षत्र", Pada="पद", Retro="वक्री".
  - Add `className={isHi ? "font-hindi" : ""}` on the Label text wrapper or on the Label's child span / th wrapper matching the pattern used elsewhere.
- **Acceptance Criteria Addressed**: AC-3
- **Test Requirements**:
  - `rule` TR-3.1: All 7 form Label elements have an `isHi ?` ternary with both English and Hindi non-empty string values, Hindi containing Devanagari characters; evidence: grep in BirthChartPage.tsx Label children
  - `rule` TR-3.2: All 6 PlanetTable `<th>` elements have an `isHi ?` ternary with both EN+HI non-empty strings; evidence: grep `<th` in BirthChartPage.tsx
  - `rule` TR-3.3: Hindi variants use `font-hindi` class on the element (immediate parent or text wrapper); evidence: source inspection
  - `rubric` TR-3.4: Label readability on mobile; scale 1-5; anchors 1 = Hindi labels overflow, 3 = readable but tight spacing, 5 = labels render cleanly in both languages with no clipping; threshold >=4

## Task 4: Prashna — Add a loading skeleton in the results-area section 3
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit `src/pages/QuestionPage.tsx` section 3 (currently line 890 "SECTION 3: Results panel" → `{result && ( <motion.div ...> ... )}`).
  - **Directly after** the closing `</Card>` of Section 2 form (line 888) and **before** the existing `{result && (` block, insert a new conditional `{loading && ( ... )}`.
  - In that block, use the standardized `ChartLoadingState` component with a bilingual message chosen via `isHi`: EN = "Analyzing your question with Prasna Marga methods…", HI = "प्रश्न मार्ग विधियों से आपका प्रश्न विश्लेषण हो रहा है…".
  - Alternatively, if height/rhythm should match the verdict card size, compose 2 stacked LoadingSkeleton blocks: `variant="card" rows={4}` for verdict + a `variant="table" rows={6}` for indicators. But prefer ChartLoadingState for consistency with MatchMaking.
  - Import `ChartLoadingState` from `@/components/ChartLoadingState` (it already exists — check ChartLoadingState.tsx).
- **Acceptance Criteria Addressed**: AC-4
- **Test Requirements**:
  - `rule` TR-4.1: QuestionPage.tsx section 3 region renders `<ChartLoadingState>` (or equivalent LoadingSkeleton composite) when `loading=true`, AND it's located visually in the same vertical column / results area where the actual verdict card will render; evidence: source code grep for `ChartLoadingState` in QuestionPage.tsx
  - `rule` TR-4.2: Loading message has an `isHi` ternary with both EN+HI strings; evidence: source inspection
  - `rubric` TR-4.3: Visual rhythm dimension; scale 1-5; anchors 1 = block overlaps with form or history, 3 = block present but wrong size/spacing, 5 = message block occupies correct vertical space matching verdict card placeholder; threshold >=4

## Task 5: Prashna — Add ChartEmptyState for pre-first-calculation empty state
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit QuestionPage.tsx section 3 area, below the loading block added in Task 4 and before `{result && ...}`.
  - Insert: `{!loading && !error && !result && (<ChartEmptyState ... />)}`
  - Import `ChartEmptyState` from `@/components/ChartEmptyState` (existing component).
  - Icon: Use `HelpCircle` from lucide-react (already imported on L26 of QuestionPage.tsx — no new import needed).
  - Bilingual title: HI = "अभी कोई प्रश्न विश्लेषण नहीं हुआ", EN = "No question analyzed yet".
  - Bilingual description: HI = "ऊपर अपना प्रश्न दर्ज करें और 'फलादेश पाएं' दबाएं — प्रश्न समय की कुंडली, राशि-विश्लेषण, और प्रश्न मार्ग मीटर दिखेंगे।", EN = "Enter your question above and click Get Phaladesh. You'll see the Prashna chart, verdict summary, and Prasna Marga strength meters.".
  - Optional action: pass a Button-sized CTA hint that scrolls focus to the textarea id of the question input (optional, bonus if easy).
- **Acceptance Criteria Addressed**: AC-5
- **Test Requirements**:
  - `rule` TR-5.1: QuestionPage.tsx has a `{!loading && !error && !result && (...)}` branch with `<ChartEmptyState>` render in section 3 area; evidence: source grep
  - `rule` TR-5.2: Both title and description props have `isHi` ternary with both EN+HI non-empty variants; evidence: source inspection
  - `rubric` TR-5.3: Aesthetics dimension; scale 1-5; anchors 1 = empty state looks broken or out-of-place, 3 = present but copy too long / icon mismatched, 5 = inviting, clearly prompts user, uses consistent icon and spacing with MatchMaking empty pattern; threshold >=4

## Task 6: Prashna — Replace bare Alert error block with ChartErrorState + retry
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit QuestionPage.tsx lines 880-886 (the inline Alert variant=destructive block after submit button).
  - Delete or replace that bare Alert with `<ChartErrorState message={error} onRetry={retryHandler} />`.
  - Implement `retryHandler`: check if form state is valid (question is non-empty). If user has already typed a question (`question.trim() !== ""`), call `onSubmit(false)` to re-analyze; otherwise just call `setError(null)` and scroll/focus to the question textarea.
  - Move this error block cleanly into section 3 results area (e.g. as the first conditional after the empty-state and before `{result && ...}`) OR keep inline below form — choose whichever maintains visual flow (within section 3 is stronger pattern per BirthChartPage).
  - Import `ChartErrorState` from `@/components/ChartErrorState` (existing component, no new file needed).
- **Acceptance Criteria Addressed**: AC-6
- **Test Requirements**:
  - `rule` TR-6.1: Bare `<Alert variant="destructive">` (L880-886) no longer renders error output; `<ChartErrorState>` is used instead; evidence: source grep
  - `rule` TR-6.2: `onRetry` handler provided that either calls `onSubmit` when question exists or clears error + focuses question input; evidence: handler definition in source code
  - `rubric` TR-6.3: Error recovery UX dimension; scale 1-5; anchors 1 = user can't recover without navigating back, 3 = retry works but placement jarring, 5 = block clearly visible with obvious Try Again button in results area; threshold >=4

## Task 7: Prashna — Fix 4-tab TabsList responsive overflow at <640px
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit QuestionPage.tsx line 960: `TabsList className="grid w-full grid-cols-4"`.
  - Replace with responsive pattern: `"grid grid-cols-2 gap-1 sm:grid-cols-4 sm:gap-2 w-full"`.
  - Verify Hindi tabs at 360px don't clip — if the "शास्त्रीय फलादेश" (Classical Reading) Hindi label wraps awkwardly, add `TabsTrigger` class `text-xs` or `whitespace-normal` or reduce padding with `shrink-0 min-w-0` pattern. The goal is no horizontal scroll and text either truncates with `text-ellipsis` or wraps without buttons overlapping.
- **Acceptance Criteria Addressed**: AC-7
- **Test Requirements**:
  - `rule` TR-7.1: TabsList className includes `grid-cols-2` AND `sm:grid-cols-4` (or equivalent responsive breakdown); evidence: source code of QuestionPage.tsx L960 area
  - `rule` TR-7.2: No horizontal scroll on 360px viewport; evidence: devtools responsive inspection at 360px width
  - `rubric` TR-7.3: Tab readability dimension; scale 1-5; anchors 1 = tabs wrap so labels are clipped / unreadable, 3 = tabs wrap to 2 cols but uneven, 5 = clean 2-col wrap on mobile with readable triggers and no overflow; threshold >=4

## Task 8: Prashna — Replace plain "Loading classical reading…" text with structured LoadingSkeleton
- **Status**: `pending`
- **Priority**: medium
- **Depends On**: None
- **Description**:
  - Edit QuestionPage.tsx lines 1330-1336 inside classical tab content: the `!classicalAnswer` fallback branch.
  - Import `LoadingSkeleton` from `@/components/ui/loading-skeleton` if not yet imported on the page.
  - Replace the plain text div with 3 or 5 stacked skeleton blocks that preview the 5-section structure:
    1. `LoadingSkeleton variant="card" rows={3}` — previews Direct Answer section (1. Direct Answer + 2 badges)
    2. `LoadingSkeleton variant="card" rows={5}` — previews Astrological Reasoning section
    3. `LoadingSkeleton variant="card" rows={4}` — previews Timing Windows section
    4. `LoadingSkeleton variant="card" rows={3}` — previews Risks section
    5. `LoadingSkeleton variant="card" rows={4}` — previews Remedies section
  - Wrap 5 skeletons in a `space-y-4` container for consistent spacing matching the actual content rhythm.
  - Optionally surround with role="status" aria-live="polite" for accessibility.
- **Acceptance Criteria Addressed**: AC-8
- **Test Requirements**:
  - `rule` TR-8.1: Classical tab's `!classicalAnswer` branch no longer contains plain "Loading classical reading…" text; evidence: source code grep
  - `rule` TR-8.2: Contains >= 3 distinct `<LoadingSkeleton>` invocations; evidence: source code count of LoadingSkeleton tags >=3 in the classical fallback area
  - `rubric` TR-8.3: Skeleton faithfulness dimension; scale 1-5; anchors 1 = single line, 3 = generic 2 card skeletons, 5 = 5 sections clearly previewed matching the final render's vertical proportions; threshold >=4

## Task 9: Prashna — Fix vertical section spacing rhythm (Notice → Research → Form → Results → History)
- **Status**: `pending`
- **Priority**: medium
- **Depends On**: None
- **Description**:
  - Inspect the direct children of `div.max-w-5xl` container (QuestionPage.tsx L504-1443). Current order:
    1. Header row (L506): no explicit mb
    2. Motion hero (L520): `mb-8` — OK
    3. ValidationInProgressNotice (L542): no margin bottom
    4. UniversalPrasnaResearch (L545): no margin (no wrapper classname at render site)
    5. Form Card (L548): `mb-6` — OK
    6. Section 3 (Result block at L890): no top-level wrapper margin (wraps only result)
    7. Section 4 Reading History (L1437): `mt-8` — OK
  - Apply the following minimal fixes to add breathing room:
    - Wrap `<ValidationInProgressNotice>` (L542) in `<div className="mb-6">` or append `className="mb-6"` directly (if component accepts it — check PrototypeStatusBanner). If the component does NOT accept `className`, wrap in a div.
    - Wrap `<UniversalPrasnaResearch .../>` (L545) in `<div className="mb-6">` to separate research bar from Form Card below.
  - Do NOT touch Form mb-6, Section 4 mt-8 — those are already good.
- **Acceptance Criteria Addressed**: AC-9
- **Test Requirements**:
  - `rule` TR-9.1: ValidationInProgressNotice at L542 has equivalent `mb-6` bottom separation from UniversalPrasnaResearch (either directly via className prop or wrapping div); evidence: source inspection
  - `rule` TR-9.2: UniversalPrasnaResearch at L545 has equivalent `mb-6` bottom separation from the Form Card (L548); evidence: source inspection
  - `rubric` TR-9.3: Vertical rhythm consistency dimension; scale 1-5; anchors 1 = some sections still butt directly, 3 = margin added but too large/small, 5 = clean evenly-spaced top-to-bottom flow with consistent 24px step gaps between major sections; threshold >=4

## Task 10: Prashna — Soften absolute "90% higher predictive precision" trust claim in birth prompt
- **Status**: `pending`
- **Priority**: high
- **Depends On**: None
- **Description**:
  - Edit QuestionPage.tsx lines 818-822 (birth prompt body paragraph).
  - Current English: "Providing your birth details enables combined Natal Chart + Gochar (Transit) calculations. **This yields up to 90% higher predictive precision.** Would you like to enter your birth details?"
  - Current Hindi: line 819 — contains the Hindi equivalent 90% claim.
  - Soften to a conservative, evidence-appropriate framing. Do NOT use any percentage numbers. Do NOT use "precision" guarantees. Prefer framing like:
    - EN replacement: *"Providing your birth details enables a combined Natal Chart + Gochar (Transit) analysis, producing a more complete and grounded reading. Would you like to enter your birth details?"*
    - HI replacement: *"अपना जन्म विवरण देने से जन्म कुंडली + गोचर का संयुक्त विश्लेषण संभव होता है, जिससे अधिक पूर्ण और सुदृढ़ फलादेश मिलता है। क्या आप अपना जन्म विवरण दर्ज करना चाहेंगे?"*
  - Keep the existing structure: same p className, same font-hindi class, only change the copy strings.
- **Acceptance Criteria Addressed**: AC-10
- **Test Requirements**:
  - `rule` TR-10.1: Birth prompt paragraph in both language variants does NOT contain any digit (0-9) percentage (regex \d+% in the string content) and does NOT contain "precision" / "predictive precision" English equivalents; evidence: source code text search
  - `rule` TR-10.2: Hindi and English prompt paragraphs still contain both language strings; evidence: `isHi ?` ternary still present and both branches non-empty
  - `rubric` TR-10.3: Messaging softening dimension; scale 1-5; anchors 1 = still sounds like absolute guarantee, 3 = neutral but generic, 5 = honest, transparent, still motivating without numeric or precision claims; threshold >=4

## Task 11: Run production build, fix any TypeScript / lint errors
- **Status**: `pending`
- **Priority**: high
- **Depends On**: Task 1, Task 2, Task 3, Task 4, Task 5, Task 6, Task 7, Task 8, Task 9, Task 10
- **Description**:
  - Execute `npm run build` from the project root.
  - Fix any TypeScript strict-mode errors, unused imports, or Vite bundling warnings that originate from the 10 changed files (not from pre-existing issues).
  - Re-run until exit code 0.
- **Acceptance Criteria Addressed**: AC-11
- **Test Requirements**:
  - `rule` TR-11.1: `npm run build` exits with code 0 and last 10 output lines contain "✓ built in …"; evidence: terminal output capture
