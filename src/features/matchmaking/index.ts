/**
 * Matchmaking (Kundli Milan) feature barrel.
 *
 * Public surface of the matchmaking feature module.
 * Import from '@/features/matchmaking' instead of reaching into subdirectories.
 */

export type { CompatibilityInput, GunaMilanResult, CompatibilityResult } from './types';
